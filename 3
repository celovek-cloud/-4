#Поиск максимума

flag = True
max = 0
while flag:
    num = int(input('Введите число: '))
    if num == 0:
        flag = False
    else:
        if num > max:
            max = num
        else:
            max == max
print(f'Наибольшее число: {max}')
